# Успешное завершение печати
exit

# Задержка
delay u16

# Установить максимальную дельту регулятора
set_motors_speed u8

# Установить прогресс печати в процентах
set_progress u8

# Установить мультипликатор скорости
set_speed_multiplication u16

# Переместиться в точку (x, y)
move_to i16 i16
